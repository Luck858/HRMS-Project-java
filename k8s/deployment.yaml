apiVersion: apps/v1
kind: Deployment
metadata:
  name: hrms-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: springboot
  template:
    metadata:
      labels:
        app: springboot
    spec:
      containers:
      - name: springboot
        image: lucky89788/hrms:latest
        ports:
        - containerPort: 8080